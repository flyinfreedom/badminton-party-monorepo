import { ConsumptionPatternsPipe } from './consumption-patterns.pipe';

describe('ConsumptionPatternsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsumptionPatternsPipe();
    expect(pipe).toBeTruthy();
  });
});
